// Web mock for react-native-get-random-values
// Web browsers already have native crypto.getRandomValues - no polyfill needed
// This empty file prevents the import.meta error from the native package
